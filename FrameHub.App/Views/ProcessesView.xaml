<UserControl x:Class="FrameHub.App.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.25*" />
            <ColumnDefinition Width="0.85*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <Border Width="36" Height="36" CornerRadius="12" Background="{StaticResource BrushPrimarySoft}" Margin="0,0,12,0">
                                <TextBlock Text="&#xE9D9;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{StaticResource BrushPrimary}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding ProcessListTitle}" Style="{StaticResource SectionTitleText}" />
                                <TextBlock Text="{Binding StatusText}" Style="{StaticResource SmallMutedText}" Margin="0,3,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Content="Refresh" VerticalAlignment="Top" />
                </Grid>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Processes}"
                          SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{Binding DataContext.NameHeader, RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding DisplayName}" Width="*" />
                        <DataGridTextColumn Header="{Binding DataContext.InstancesHeader, RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding InstanceCount}" Width="72" />
                        <DataGridTextColumn Header="{Binding DataContext.PriorityHeader, RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding Priority}" Width="145" />
                        <DataGridTextColumn Header="{Binding DataContext.RamHeader, RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding RamUsageMB}" Width="110" />
                        <DataGridTextColumn Header="{Binding DataContext.CpuHeader, RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding CpuUsage}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource CardBorder}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Grid Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="36" Height="36" CornerRadius="12" Background="#142642" Margin="0,0,12,0">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{StaticResource BrushCyan}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding SettingsTitle}" Style="{StaticResource SectionTitleText}" />
                            <TextBlock Text="{Binding SettingsSubtitle}" Style="{StaticResource SmallMutedText}" TextWrapping="Wrap" Margin="0,3,0,0" />
                        </StackPanel>
                    </Grid>

                    <Border Style="{StaticResource SoftCardBorder}" Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock Text="{Binding ModeLabel}" Style="{StaticResource CardTitleText}" Margin="0,0,0,8" />
                            <ComboBox ItemsSource="{Binding AvailableOptimizationModes}"
                                      SelectedItem="{Binding SelectedOptimizationMode}" />

                            <TextBlock Text="{Binding PriorityLabel}" Style="{StaticResource CardTitleText}" Margin="0,16,0,8" />
                            <ComboBox ItemsSource="{Binding AvailablePriorities}"
                                      SelectedItem="{Binding SelectedPriority}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SoftCardBorder}">
                        <StackPanel>
                            <TextBlock Text="{Binding CoreSelectionTitle}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" />
                            <WrapPanel Margin="0,0,0,10">
                                <Button Style="{StaticResource SecondaryButton}" Content="{Binding SelectAllText}" Command="{Binding SelectAllCommand}" Margin="0,0,8,8" />
                                <Button Style="{StaticResource SecondaryButton}" Content="{Binding ClearAllText}" Command="{Binding ClearAllCommand}" Margin="0,0,8,8" />
                                <Button Style="{StaticResource SecondaryButton}" Content="{Binding DisableSmtText}" Command="{Binding DisableSmtCommand}" Margin="0,0,8,8" />
                                <Button Style="{StaticResource SecondaryButton}" Content="{Binding DisableECoresText}" Command="{Binding DisableECoresCommand}" Margin="0,0,8,8" />
                            </WrapPanel>

                            <ItemsControl ItemsSource="{Binding Cores}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#0E1A2F" BorderBrush="{StaticResource BrushBorderSoft}" BorderThickness="1" CornerRadius="13" Padding="10" Margin="0,0,8,8" Width="132">
                                            <StackPanel>
                                                <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsChecked}" />
                                                <ProgressBar Maximum="100" Value="{Binding LoadUsage}" Margin="0,9,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Grid Margin="0,18,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Style="{StaticResource PrimaryButton}" Content="{Binding ApplyButtonText}" Command="{Binding ApplyCommand}" Margin="0,0,8,0" />
                        <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="{Binding SaveProfileButtonText}" Command="{Binding SaveProfileCommand}" Margin="8,0,0,0" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
