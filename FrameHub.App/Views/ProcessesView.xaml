<UserControl x:Class="FrameHub.App.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:FrameHub.App.Helpers">
    <UserControl.Resources>
        <helpers:BindingProxy x:Key="VmProxy" Data="{Binding}" />
        <Style x:Key="ProcessMetricCell" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource FrameHubDisplayFontFamily}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="1.7*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="0.8*" /></Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource DataPanel}">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                <Grid Margin="16,14,14,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center"><TextBlock Text="{Binding ProcessListTitle}" Style="{StaticResource SectionTitleText}" /><TextBlock Text="{Binding StatusText}" Style="{StaticResource CaptionText}" Margin="0,2,0,0" /></StackPanel>
                    <Button Grid.Column="1" Style="{StaticResource ToolbarButton}" Command="{Binding RefreshCommand}" Content="{Binding RefreshButtonText}" VerticalAlignment="Center" />
                </Grid>

                <DataGrid x:Name="ProcessGrid" Grid.Row="1" Margin="1"
                          ItemsSource="{Binding Processes}" SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"
                          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                          HeadersVisibility="Column" SelectionMode="Single" EnableRowVirtualization="True" EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="{Binding Data.NameHeader, Source={StaticResource VmProxy}}" Width="2.5*" MinWidth="220"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding DisplayName}" ToolTip="{Binding DisplayName}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" FontWeight="SemiBold" /></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                        <DataGridTextColumn Header="{Binding Data.PidHeader, Source={StaticResource VmProxy}}" Binding="{Binding Id}" Width="64" />
                        <DataGridTextColumn Header="{Binding Data.InstancesHeader, Source={StaticResource VmProxy}}" Binding="{Binding InstanceCount}" Width="58" />
                        <DataGridTextColumn Header="{Binding Data.ModeHeader, Source={StaticResource VmProxy}}" Binding="{Binding ModeDisplay}" Width="95" />
                        <DataGridTextColumn Header="{Binding Data.PriorityHeader, Source={StaticResource VmProxy}}" Binding="{Binding Priority}" Width="112" />
                        <DataGridTextColumn Header="{Binding Data.CpuHeader, Source={StaticResource VmProxy}}" Binding="{Binding CpuUsage}" ElementStyle="{StaticResource ProcessMetricCell}" Width="64" />
                        <DataGridTextColumn Header="{Binding Data.RamHeader, Source={StaticResource VmProxy}}" Binding="{Binding RamUsageMB}" ElementStyle="{StaticResource ProcessMetricCell}" Width="78" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource SectionSurface}" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="{Binding SettingsTitle}" Style="{StaticResource SectionTitleText}" />
                        <TextBlock Text="{Binding SettingsSubtitle}" Style="{StaticResource SmallMutedText}" TextWrapping="Wrap" Margin="0,4,0,0" />
                    </StackPanel>

                    <TextBlock Text="{Binding ModeLabel}" Style="{StaticResource TinyCapsText}" Margin="0,0,0,6" />
                    <ComboBox ItemsSource="{Binding AvailableOptimizationModes}" SelectedItem="{Binding SelectedOptimizationModeOption}" DisplayMemberPath="DisplayName" />
                    <TextBlock Text="{Binding PriorityLabel}" Style="{StaticResource TinyCapsText}" Margin="0,16,0,6" />
                    <ComboBox ItemsSource="{Binding AvailablePriorities}" SelectedItem="{Binding SelectedPriority}" />

                    <Border Height="1" Background="{StaticResource BrushBorderSubtle}" Margin="0,24,0,16" />
                    <TextBlock Text="{Binding CoreSelectionTitle}" Style="{StaticResource DataTitleText}" Margin="0,0,0,10" />
                    <WrapPanel Margin="0,0,0,8">
                        <Button Style="{StaticResource ToolbarButton}" Content="{Binding SelectAllText}" Command="{Binding SelectAllCommand}" Margin="0,0,6,6" />
                        <Button Style="{StaticResource ToolbarButton}" Content="{Binding ClearAllText}" Command="{Binding ClearAllCommand}" Margin="0,0,6,6" />
                        <Button Style="{StaticResource ToolbarButton}" Content="{Binding DisableSmtText}" Command="{Binding DisableSmtCommand}" Margin="0,0,6,6" />
                        <Button Style="{StaticResource ToolbarButton}" Content="{Binding DisableECoresText}" Command="{Binding DisableECoresCommand}" Margin="0,0,6,6" />
                    </WrapPanel>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="8" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0"><TextBlock Text="{Binding PhysicalCoresTitle}" Style="{StaticResource CaptionText}" FontWeight="SemiBold" Margin="0,0,0,6" /><ItemsControl ItemsSource="{Binding PhysicalCores}"><ItemsControl.ItemTemplate><DataTemplate><Border Style="{StaticResource InlineRow}" Padding="0,7"><CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsChecked}" /></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
                        <StackPanel Grid.Column="2"><TextBlock Text="{Binding ThreadCoresTitle}" Style="{StaticResource CaptionText}" FontWeight="SemiBold" Margin="0,0,0,6" /><ItemsControl ItemsSource="{Binding ThreadCores}"><ItemsControl.ItemTemplate><DataTemplate><Border Style="{StaticResource InlineRow}" Padding="0,7"><CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsChecked}" /></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
                    </Grid>

                    <Border Height="1" Background="{StaticResource BrushBorderSubtle}" Margin="0,24,0,16" />
                    <Button Style="{StaticResource PrimaryButton}" Content="{Binding ApplyButtonText}" Command="{Binding ApplyCommand}" Margin="0,0,0,8" />
                    <Button Style="{StaticResource SecondaryButton}" Content="{Binding SaveProfileButtonText}" Command="{Binding SaveProfileCommand}" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
