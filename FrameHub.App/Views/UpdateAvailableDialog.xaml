<Window x:Class="FrameHub.App.Views.UpdateAvailableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FrameHub"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    <Border Background="{StaticResource BrushAppBackground}" BorderBrush="{StaticResource BrushBorderDefault}" BorderThickness="1" Margin="6" MouseLeftButtonDown="DialogRoot_MouseLeftButtonDown">
        <Border.BitmapEffect><DropShadowBitmapEffect ShadowDepth="2" Direction="270" Opacity="0.45" Softness="0.35" /></Border.BitmapEffect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="24,20,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="{StaticResource BrushAccentSoft}" Margin="0,0,14,0" BorderBrush="{StaticResource BrushBorderDefault}" BorderThickness="1" ClipToBounds="True" VerticalAlignment="Top">
                    <Image Source="/FrameHub.App;component/Assets/FrameHubLogo.png" Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" />
                </Border>
                <StackPanel Grid.Column="1" Margin="0,2,0,0">
                    <TextBlock x:Name="DialogTitleText" Style="{StaticResource SectionTitleText}" />
                    <TextBlock x:Name="DialogHeadingText" Style="{StaticResource SecondaryBodyText}" TextWrapping="Wrap" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="1" CornerRadius="8" Padding="14,10" Margin="24,16,24,0">
                <StackPanel>
                    <TextBlock x:Name="LatestVersionText" FontFamily="{StaticResource FontDisplay}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushAccentPrimary}" />
                    <TextBlock x:Name="CurrentVersionText" Style="{StaticResource SmallMutedText}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Grid Grid.Row="2" Margin="24,18,24,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="OpenReleaseButton" Grid.Column="0" Style="{StaticResource PrimaryButton}" Margin="0,0,6,0" IsDefault="True" Click="OpenReleaseButton_Click" />
                <Button x:Name="LaterButton" Grid.Column="1" Style="{StaticResource SecondaryButton}" Margin="6,0,0,0" IsCancel="True" Click="LaterButton_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
